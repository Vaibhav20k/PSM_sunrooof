import {state,signed} from '../lib/store.js';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');try{const file=req.query.file;if(!['meta.json','leads.json','calls.json','rawquote.json','dealstage.json'].includes(file))return res.status(404).json({error:'Not found'});const s=await state();if(!s.manifest)return res.status(503).json({error:'CRM initial sync pending'});if(file==='meta.json')return res.json(s.manifest);const id=req.query.snapshot||s.manifest.snapshotId;if(!/^[a-zA-Z0-9-]{1,64}$/.test(id))return res.status(400).json({error:'Invalid snapshot'});res.redirect(307,await signed(id,file));}catch{res.status(503).json({error:'CRM snapshot unavailable'});}}
